function Tile(props){
    const { tile, index, imageChoice } = props;
   
    const styleTile = {
      chosenImage: `url(${imageChoice})`,
      sizeChosenImage: `100%`
    };
  }
   export default Tile;
  
 